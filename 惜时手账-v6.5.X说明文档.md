# 惜时手账个人版 - v6.5.X 系列说明文档

**应用名称**：惜时手账个人版  
**适用平台**：Android（荣耀手机等）  
**最新版本**：v6.5.4  
**文档日期**：2026-09-05  

---

## 目录

1. [应用概述](#1-应用概述)
2. [核心功能模块](#2-核心功能模块)
3. [页面详细说明](#3-页面详细说明)
4. [v6.5.X 版本更新日志](#4-v65x-版本更新日志)
5. [使用指南](#5-使用指南)
6. [常见问题与解决方案](#6-常见问题与解决方案)
7. [技术架构](#7-技术架构)
8. [数据存储说明](#8-数据存储说明)

---

## 1. 应用概述

惜时手账个人版是一款专为个人时间管理设计的Android应用，集成了待办清单、番茄钟计时、任务规划、每日时间目标、月历视图、时间统计、记录补录、桌面小组件等功能。

### 设计理念

- **本地化优先**：所有数据存储在手机本地，不上传云端，保护隐私
- **循环目标驱动**：设置中的分类与活动自动成为每日循环待办
- **时间轴记录**：以时间轴方式展示一天的活动，空白区间可一键补录
- **多维度统计**：支持日/周/月视图，按时间、事件、分类等多维度统计

### 适用人群

- 需要精细管理每日时间的学生、科研人员
- 有固定每日目标（如工作8小时、学习2小时）的用户
- 希望记录并分析时间使用情况的用户

---

## 2. 核心功能模块

### 2.1 计时模块

| 功能 | 说明 |
|------|------|
| 正向计时 | 选择分类+活动后开始计时，可随时结束 |
| 番茄钟 | 默认25分钟工作+5分钟休息，可自定义时长 |
| 计时锁定 | 开启屏幕固定后，计时期间无法切换应用 |
| 计时提醒 | 计时结束时播放提示音+震动+系统通知 |

### 2.2 待办清单模块

| 功能 | 说明 |
|------|------|
| 循环待办 | 设置中的分类与活动自动成为每日待办 |
| 手动待办 | 支持创建单次任务，含截止日期、周期重复 |
| 子任务 | 支持任务层级，可自动生成子任务列表 |
| 优先级排序 | 按重要性排序，最重要的任务排在最前 |
| 周期重复 | 支持每天/每周（可选周几）/每月重复 |
| 日/周/月视图 | 日视图列表、周视图格子、月视图日历 |

### 2.3 任务规划模块

| 功能 | 说明 |
|------|------|
| 任务创建 | 设置任务名称、分类、预计时长 |
| 子任务拆分 | 支持6类智能拆分模板 |
| 任务进度 | 实时显示已投入时间/预计时间 |
| 任务计时 | 点击任务直接进入对应计时 |

### 2.4 时间统计模块

| 功能 | 说明 |
|------|------|
| 日视图 | 总时长、记录次数、番茄数、平均值 |
| 周视图 | 每日时长柱状图、周计划建议 |
| 月视图 | 各周时长变化、月计划建议 |
| 分类占比 | 环形饼图，显示各分类时间占比 |
| 事件统计 | 按活动统计，区分循环事件和单次事件 |
| 目标进度 | 各分类每日目标完成进度 |
| 日期切换 | 支持上一天/下一天、上一周/下一周、上一月/下一月 |

### 2.5 记录模块

| 功能 | 说明 |
|------|------|
| 时间轴展示 | 按时间顺序展示记录，高度按时长变化 |
| 空白区间补录 | 超过15分钟的空白区间显示虚线框，点击补录 |
| 实时区间 | 今天最后一条记录到当前时间的实时空白 |
| 记录编辑 | 点击已有记录可修改时间、分类、活动 |
| 补录弹窗 | 直接选择分类活动/任务/待办，无需手动填写 |

### 2.6 设置模块

| 功能 | 说明 |
|------|------|
| 分类与活动管理 | 增删改分类（工作/学习/锻炼等）及下属活动 |
| 每日时间目标 | 为每个分类设置每日目标分钟数 |
| 番茄钟设置 | 自定义工作时长、休息时长 |
| 应用信息 | 版本号、隐私说明 |

### 2.7 桌面小组件

| 功能 | 说明 |
|------|------|
| 今日任务展示 | 显示前5个未完成任务 |
| 点击直接计时 | 点击任务直接打开应用并进入计时 |
| 白色主题 | 白色背景+深色文字，适配各种桌面 |
| 自动同步 | 应用启动时自动同步任务数据到小组件 |

---

## 3. 页面详细说明

### 3.1 计时页

**入口**：底部导航第一个按钮

**布局**：
- 顶部：分类选择（下拉列表，两级：大类→活动）
- 中部：计时显示（大号数字，分:秒）
- 底部：开始/暂停/结束按钮
- 番茄钟模式切换按钮

**操作流程**：
1. 选择分类（如"工作"）
2. 选择活动（如"论文撰写"）
3. 点击"开始计时"或"番茄钟"
4. 计时结束后自动保存记录
5. 番茄钟结束后提示休息

**计时锁定**：
- 首次使用需在系统设置中开启"屏幕固定"
- 开启后计时期间无法返回桌面或切换应用
- 结束计时后自动解锁

### 3.2 待办页

**入口**：底部导航第二个按钮

**日视图**：
- 顶部：24小时时间条（标注睡觉、午休区间）
- 大环形目标进度图（总目标完成百分比）
- 各分类进度条（可展开显示二级活动）
- 今日任务列表（循环待办+手动待办）
- 任务左侧色条表示分类，右侧圆圈标记完成
- 点击任务区域进入计时，点击圆圈标记完成

**周视图**：
- 7个小格子（周一至周日）
- 周末格子颜色稍暗以区分
- 每个格子显示任务数量和预览
- 点击格子放大跨整行，显示当天任务列表

**月视图**：
- 整月日历（30/31个格子，7列布局）
- 每个日期显示任务数量
- 点击日期显示当天任务列表

**底部选项**：
- 新建待办按钮
- 显示筛选（全部/未完成/已完成）
- 排序方式（时间/优先级）

### 3.3 时间页（统计+记录融合）

**入口**：底部导航第三个按钮

**顶部导航**：
- 日/周/月切换tab
- 日期导航栏（‹ 日期 ›），支持左右切换

**日视图内容**：
1. **统计概览**：总时长、记录次数、番茄数、平均时长
2. **分类占比**：环形饼图，中心显示总时长
3. **事件统计**：按活动统计，带"循环"/"单次"标签
4. **目标进度**：各分类目标完成情况，可展开二级活动
5. **任务进度**：进行中任务的投入时间进度
6. **详细记录**：时间轴样式，含空白区间补录

**周视图内容**：
1. 统计概览（本周汇总）
2. 每日时长柱状图
3. 计划建议（各分类距目标差距、每日建议）
4. 本周记录列表（倒序）

**月视图内容**：
1. 统计概览（本月汇总）
2. 各周时长变化柱状图
3. 计划建议（周环比、目标完成率）
4. 本月记录列表（最近30条，倒序）

**记录时间轴说明**：
- 记录按时间正序排列（最早的在上面）
- 每条记录高度按时长变化：<30分钟=60px，30-60分钟=80px，>60分钟=100px
- 两条记录间≥15分钟的空白显示虚线框，标注"空白 X分钟，点击补录"
- 今天最后一条记录到当前时间显示蓝色高亮空白框
- 空白区间高度按时长比例（40-120px）
- 点击空白区间弹出补录弹窗
- 点击已有记录弹出编辑弹窗

### 3.4 设置页

**入口**：底部导航第四个按钮

**分类与活动管理**：
- 大类列表（可展开/收起）
- 大类后直接输入每日目标分钟数
- 小类通过滑动条设置时间（显示百分比和实际分钟数）
- 小三角展开按钮和删除按钮有足够间距，防止误触
- 新增的大类和小类自动成为每日循环待办

**番茄钟设置**：
- 工作时长（分钟）
- 短休息时长（分钟）
- 长休息时长（分钟）
- 保存按钮

**关于**：
- 应用名称和版本号
- 本地存储说明

---

## 4. v6.5.X 版本更新日志

### v6.5.4（2026-09-05）

**修复**：
- **日期切换按钮不工作**：之前版本只在HTML中添加了‹ ›按钮，但JS中的点击事件绑定未成功写入（脚本替换失败未报错），导致按钮存在但点击无反应。已在`bindEvents()`中正确添加事件：
  - `‹`按钮：`state.statOffset--`，切换到上一天/上一周/上一月
  - `›`按钮：`state.statOffset++`，切换到下一天/下一周/下一月
  - 切换日/周/月tab时自动重置偏移为0
- **补录功能全面检查**：确认空白区间显示、点击补录、保存刷新全流程正常

**文件变更**：
- `js/app.js`：bindEvents函数中添加stat-prev/stat-next事件绑定

---

### v6.5.3（2026-09-05）

**修复**：
- **日期导航栏显示"NaN年NaN月"**：根本原因是`state`对象中缺少`statOffset`属性，导致`state.statOffset`为`undefined`，`now.getMonth() + undefined = NaN`，`setMonth(NaN)`使日期对象失效，`getFullYear()`返回NaN。
  - 在state对象中添加`statOffset: 0`
  - 在renderStats开头添加双重防御：`if (typeof state.statOffset !== 'number' || isNaN(state.statOffset)) state.statOffset = 0;`

**文件变更**：
- `js/app.js`：state对象添加statOffset属性，renderStats添加防御代码

---

### v6.5.2（2026-09-05）

**修复**：
- **NaN/undefined乱码全面修复**（共10处防御性代码）：
  1. 目标进度百分比计算：防止targetMin为0导致除零
  2. 目标进度二级活动时长统计：添加`Number(r.duration) || 0`
  3. 任务进度已投入时长显示：添加`Number(usedMin) || 0`
  4. 记录列表时长显示和高度计算：添加`Number(r.duration) || 0`
  5. 周计划建议百分比计算：添加`Number(weekUsed) || 0`
  6. 周计划建议每日建议计算：防止daysLeft为0
  7. 月计划建议百分比计算：添加`Number(monthTotal) || 0`
  8. 统计概览平均值计算：防止记录数为0
  9. 所有reduce求和：统一添加`Number(r.duration) || 0`
  10. 柱状图数值计算：添加`Number(d.value) || 0`

- **补录功能重构为时间轴样式**：
  - 之前：点击"+ 补录记录"按钮，默认补录当前时间前1小时
  - 现在：记录列表按时间正序排列，两条记录间≥15分钟的空白显示虚线框，点击空白区间直接补录该时间段
  - 空白区间高度按时长比例变化（40-120px）
  - 今天最后一条记录到当前时间显示蓝色高亮实时空白
  - 空白区间过长（>2小时）时默认补录中间1小时

- **桌面Widget兼容性修复**：
  - Widget布局中使用了`paddingVertical`属性（API 26+才支持），在部分荣耀手机上导致Widget加载失败、显示"无法添加小部件"
  - 改为`paddingTop`+`paddingBottom`，兼容所有Android版本

**新增CSS**：
- `.record-gap`：空白区间样式（虚线框、可点击）
- `.record-gap-now`：实时空白区间样式（蓝色高亮）

**文件变更**：
- `js/app.js`：renderStatsRecords函数重写，10处NaN防御
- `css/style.css`：添加record-gap相关样式
- `res/layout/widget_task.xml`：paddingVertical改为paddingTop/paddingBottom

---

### v6.5.1（2026-09-05）

**修复**：
- **界面乱码问题**：代码中使用了7个emoji字符，在部分Android设备（尤其是荣耀手机的某些系统版本）上无法正常渲染，显示为方框或乱码。全部替换为纯文字符号：
  - 🍅番茄 → `[番茄]`
  - 📋任务 → `[任务]`
  - 📌提示 → `[提示]`
  - 📈上升 → `[上升]`
  - 📉下降 → `[下降]`
  - 🔒锁 → `[锁]`
  - 📊统计 → `统计`
  - ⭐超额 → `[超额]`
  - ✅完成 → `[完成]`
  - ⏰提醒 → `[提醒]`
  - ✓对勾 → `[完成]`

- **版本号系统**：
  - 设置页→关于：显示"惜时手账 v6.5.1"
  - APK文件名：`惜时手账-v6.5.1.apk`
  - 后续每个版本都在文件名和应用内标注版本号

**文件变更**：
- `js/app.js`：替换所有emoji为文字符号，添加APP_VERSION常量
- `index.html`：关于部分添加版本号span，替换导航图标emoji

---

## 5. 使用指南

### 5.1 首次使用设置

1. **安装应用**：下载APK文件，在手机上允许安装未知来源应用
2. **设置分类与活动**：
   - 进入设置页，检查默认分类（工作/学习/锻炼/休息/娱乐/其他）
   - 根据需要增删分类和活动
   - 为每个分类设置每日目标分钟数（大类后直接输入，小类拖动滑动条）
3. **设置番茄钟**：根据习惯调整工作/休息时长
4. **添加桌面小组件**（可选）：
   - 长按桌面空白处→添加小组件→找到"惜时手账"
   - 选择合适尺寸添加到桌面

### 5.2 日常使用流程

**早晨**：
1. 打开待办页，查看今日循环待办和手动待办
2. 按优先级安排今天的任务

**工作中**：
1. 进入计时页，选择分类和活动
2. 开始计时（正向计时或番茄钟）
3. 需要专注时开启计时锁定
4. 计时结束后自动保存记录

**临时任务**：
1. 在待办页新建待办，设置截止日期和优先级
2. 点击任务直接进入计时

**补录记录**：
1. 进入时间页，查看记录时间轴
2. 找到空白区间（虚线框）
3. 点击空白区间，选择分类活动/任务/待办
4. 自动补录该时间段

**晚上/周末**：
1. 进入时间页，查看日/周统计
2. 查看各分类目标完成情况
3. 查看计划建议，调整后续时间分配

### 5.3 桌面小组件使用

1. 添加小组件到桌面
2. 小组件显示今日前5个未完成任务
3. 点击任务直接打开应用并进入对应计时
4. 任务数据在应用启动时自动同步

### 5.4 计时锁定使用

1. 首次使用：系统设置→安全→屏幕固定→开启
2. 在计时页开始计时后，点击锁定按钮
3. 计时期间无法切换应用或返回桌面
4. 结束计时后自动解锁

---

## 6. 常见问题与解决方案

### Q1：安装时提示"未知来源应用"
**A**：这是正常现象。请在手机设置中允许安装未知来源应用，或在安装提示中点击"仍要安装"。

### Q2：桌面小组件显示"无法添加小部件"
**A**：v6.5.2已修复此问题。请升级到v6.5.2或更高版本。如果仍有问题，请尝试：
- 重启手机后再添加
- 确保应用已安装在手机内存（非SD卡）
- 清除启动器缓存

### Q3：计时结束没有声音或震动
**A**：请检查：
- 手机是否开启了通知权限（设置→应用→惜时手账→通知）
- 手机是否处于静音或勿扰模式
- 应用内通知渠道"计时提醒"是否开启

### Q4：计时锁定无法开启
**A**：需要先在系统设置中开启"屏幕固定"功能：
- 设置→安全和隐私→更多安全设置→屏幕固定
- 不同品牌手机路径可能略有不同

### Q5：统计页面显示NaN
**A**：v6.5.3已修复此问题。请升级到v6.5.3或更高版本。NaN是由于日期偏移量未初始化导致的。

### Q6：日期切换箭头点击没反应
**A**：v6.5.4已修复此问题。请升级到v6.5.4或更高版本。

### Q7：补录后记录不显示
**A**：请确认：
- 补录的日期是否是当前查看的日期（可通过日期导航切换查看）
- 补录的时间区间是否与已有记录重叠
- 升级到最新版本，v6.5.2后补录后会自动刷新页面

### Q8：数据会丢失吗？
**A**：所有数据存储在手机本地的localStorage中。卸载应用会清除数据。建议定期在设置中查看数据状态。未来版本可添加导出备份功能。

### Q9：可以在多台设备间同步吗？
**A**：当前版本仅支持本地存储，不支持云端同步。所有数据仅保存在当前手机上。

### Q10：如何删除某条记录？
**A**：在时间页点击该记录，弹出编辑窗口，可修改或删除记录。

---

## 7. 技术架构

### 7.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML5 + CSS3 + JavaScript (ES6+) | 单页应用，无框架依赖 |
| 打包 | Capacitor 5 | 将Web应用打包为Android APK |
| 原生功能 | Capacitor Plugins | 本地通知、震动等 |
| 构建 | Gradle + Android SDK 34 | Android原生构建 |
| JDK | JDK 21 | Java编译环境 |

### 7.2 项目结构

```
time-manager/                    # PWA源码目录
├── index.html                   # 主页面（含所有页面结构）
├── css/
│   └── style.css                # 样式文件
├── js/
│   └── app.js                   # 应用逻辑（约3300行）
└── manifest.json                # PWA清单

time-manager-app/                # Capacitor项目目录
├── www/
│   └── index.html               # 构建生成的单文件（CSS/JS内联）
└── android/
    ├── app/
    │   └── src/main/
    │       ├── assets/public/
    │       │   └── index.html   # Android assets中的页面（必须手动复制）
    │       ├── java/com/timemanager/app/
    │       │   ├── MainActivity.java    # 主Activity（含LockTask和Widget接口）
    │       │   └── TaskWidgetProvider.java  # 桌面小组件Provider
    │       ├── res/
    │       │   ├── layout/widget_task.xml    # Widget布局
    │       │   ├── xml/widget_task_info.xml  # Widget配置
    │       │   ├── drawable/widget_background.xml  # Widget背景
    │       │   └── values/strings.xml        # 字符串资源
    │       └── AndroidManifest.xml           # 清单文件
    └── build/outputs/apk/release/
        └── app-release.apk         # 构建产物
```

### 7.3 核心数据结构

**分类（Category）**：
```javascript
{
  id: "work",              // 分类ID
  name: "工作",             // 分类名称
  color: "#007AFF",        // 分类颜色
  activities: ["实验开展", "论文撰写", ...]  // 下属活动列表
}
```

**记录（Record）**：
```javascript
{
  id: "1234567890",        // 唯一ID（时间戳）
  app: "论文撰写",          // 活动名称
  categoryId: "work",      // 分类ID
  taskId: null,            // 关联任务ID（可选）
  duration: 45,            // 时长（分钟）
  isPomodoro: false,       // 是否番茄钟
  pomodoroNumber: 0,       // 番茄钟序号
  startTime: "2026-09-05T09:00:00.000Z",  // 开始时间（ISO）
  endTime: "2026-09-05T09:45:00.000Z",    // 结束时间（ISO）
  date: "2026-09-05",      // 日期（YYYY-MM-DD）
  note: ""                 // 备注
}
```

**待办（Todo）**：
```javascript
{
  id: "todo_123",          // 唯一ID
  title: "完成论文初稿",    // 任务标题
  categoryId: "work",      // 分类ID
  completed: false,        // 是否完成
  dueDate: "2026-09-10",   // 截止日期（可选）
  dueTime: "18:00",        // 截止时间（可选）
  repeat: "weekly",        // 重复类型：daily/weekly/monthly/none
  repeatDays: [1, 3, 5],   // 每周重复的周几（0=周日）
  priority: 2,             // 优先级（0-3，越大越重要）
  parentId: null,          // 父任务ID（子任务时使用）
  autoGenerated: false     // 是否自动生成的循环待办
}
```

**目标（Goal）**：
```javascript
{
  id: "goal_work",         // 目标ID
  categoryId: "work",      // 分类ID
  targetMin: 480,          // 每日目标分钟数
  period: "daily",         // 周期（目前仅支持daily）
  enabled: true,           // 是否启用
  activityGoals: {}        // 活动级目标（可选）
}
```

### 7.4 桌面小组件实现机制

1. **数据同步**：应用启动时，JS通过`window.AndroidWidget.updateTodayTasks(json)`将今日任务数据写入Android SharedPreferences
2. **Widget更新**：写入数据后调用`TaskWidgetProvider.updateAllWidgets()`刷新所有小组件
3. **点击跳转**：Widget点击发送`com.timemanager.app.START_TIMER`广播，MainActivity接收后通过`window.__widgetTimerData`传递参数给JS
4. **JS处理**：`checkWidgetTimerData()`在启动后800ms读取参数，自动跳转到计时页并选择对应分类活动

### 7.5 计时锁定实现机制

1. **Android LockTask模式**：在MainActivity中调用`startLockTask()`，将应用固定在前台
2. **JavascriptInterface**：JS通过`window.AndroidLock.startLockTask()`调用原生锁定
3. **Manifest配置**：`lockTaskMode="if_whitelisted"`，需用户在系统设置中开启屏幕固定
4. **返回键拦截**：锁定期间拦截返回键，防止误操作退出

---

## 8. 数据存储说明

### 8.1 存储方式

所有数据使用浏览器`localStorage`存储，键值对如下：

| 键名 | 说明 |
|------|------|
| `categories` | 分类与活动列表 |
| `records` | 计时记录列表 |
| `todos` | 待办事项列表 |
| `goals` | 每日时间目标 |
| `tasks` | 任务规划列表 |
| `pomo_config` | 番茄钟配置 |
| `active_timer` | 当前活跃计时状态 |
| `manual_auto_done_YYYY-MM-DD` | 某日手动完成的循环待办 |
| `lock_task_notice_shown` | 屏幕固定提示是否已显示 |

### 8.2 数据安全

- 所有数据仅存储在手机本地，不上传任何服务器
- 卸载应用会清除所有数据
- 建议定期截图保存重要统计数据
- 未来版本可考虑添加导出JSON备份功能

### 8.3 数据迁移

当前版本不支持跨设备数据迁移。如需更换手机，请手动记录重要数据。

---

## 附录：版本号对照表

| 版本 | 主要变更 | 发布日期 |
|------|----------|----------|
| v6.5.1 | 修复emoji乱码，添加版本号系统 | 2026-09-05 |
| v6.5.2 | 全面修复NaN，补录改时间轴，Widget兼容 | 2026-09-05 |
| v6.5.3 | 修复日期NaN（statOffset缺失） | 2026-09-05 |
| v6.5.4 | 修复日期切换按钮不工作 | 2026-09-05 |

---

**文档结束**

如有问题或建议，请在使用过程中记录具体现象，以便精准定位和修复。
